# Agent Registry

Use this folder to register agent endpoints/configurations for consumption by MCP servers or other orchestrators.

Current state: no agents registered yet. Add JSON/YAML files here as needed (e.g., `agents.json`).

